<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.StepSync" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Core colors -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorOnPrimary">@color/background_color</item>
        <item name="colorSurface">@color/background_color</item>
        <item name="colorOnSurface">@color/text_color</item>
        <item name="android:colorBackground">@color/background_color</item>

        <!-- Button colors -->
        <item name="buttonTint">@color/primary_color</item>

        <!-- System bars -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Optional: App bar text/icon color -->
        <item name="toolbarStyle">@style/Widget.Material3.Toolbar</item>
    </style>
</resources>
